databases:
  - name: todos
    databaseName: todos
    user: todos
    plan: free

services:
  - type: web
    name: todos
    runtime: ruby
    plan: free
    buildCommand: "./bin/render-build.sh"
    startCommand: "rackup -p $PORT"
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: todos
          property: connectionString